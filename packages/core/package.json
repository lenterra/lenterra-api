{
  "name": "@lenterra/core",
  "version": "0.1.0",
  "description": "Pure rules engines, adaptive maths, and shared types. Runs in Hermes (app), goja (Nakama), and Node (tests).",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "test": "node --test test/",
    "guard:deps": "node ../../scripts/deps-guard.mjs"
  },
  "//dependencies": "TRD-ENG-001: this package has no runtime dependencies, ever. scripts/deps-guard.mjs fails CI if one appears.",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
